import assert from 'assert'
import should from 'should'
import { Sparse } from '../lib'    

describe('Sparse', () => {
  it('', () => {
    const z = new Sparse([[1,0,0],[5,2,0],[1,0,0]])
    throw z;
  })
})
